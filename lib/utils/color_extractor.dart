import 'package:flutter/material.dart';

class ColorExtractor {
  // Đã lược bỏ logic phức tạp để không cần dùng thư viện nặng
  static Future<Color> extractPrimaryColor(String? imagePath) async {
    return const Color(0xFF1DB954); // Trả về màu Spotify mặc định
  }
}
